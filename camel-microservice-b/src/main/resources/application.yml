spring:
  application.name: camelRecord-app
  datasource:
    url: jdbc:mysql://localhost:3306/camelrecorddb
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect